eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: gateway
zuul:
  routes:
    api-a:
      path: /questionnire/**
      serviceId: questionnire